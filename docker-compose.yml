services:
  devops:
    build: .
    image: devops:latest
    restart: always
    ports:
      - "8085:8085"
    networks:
      - devopsNet

networks:
  devopsNet:
    external: true

#> docker compose up --build -d